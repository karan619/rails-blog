<% provide :title, "#{@user} > Profile"%>
<div class="card-panel">
  <h3>
  Name: <%= @user.name%> <br/>
  Email: <%= @user.email %>
  </h3>

</div>
  <% @user.posts.each do |p| %>
  <%= render 'shared/posts', p: p %>
  <% end %>