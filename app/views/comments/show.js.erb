a = $("#modaltrigger");
a.click();
$('.modal-content').html("<%= escape_javascript(render('shared/comments', comments: @comments)) %>");
$('#postAction<%= @post.id %>').html("<%= escape_javascript(render('shared/postAction', p: @post)) %>");
$('.modal-close').on('click', function(argument) {
  $('.modal-content').html('');
})
